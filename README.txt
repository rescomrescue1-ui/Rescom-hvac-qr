RES-COM HVAC QR v19.0 — BUTTON SOUNDS

NEW
- Short click/tap sound on app buttons.
- Button Sounds toggle in Settings.
- Sound preference is saved separately on each device.
- Button sounds are ON by default.

The sound is generated inside the app, so no separate audio file is required.

Keep the SAME GitHub Pages URL so existing QR labels continue to work.
Upload version.json and service-worker.js with the rest of the files.
